---
name: run-2026-08-05-run2
description: Notes from Repo Assist run on 2026-08-05 (run id 31017959495) — bundled coding+testing PR on transcript (Phase 2 of #527: TranscriptLine per-word spans + TranscriptWord model + 13 tests).
metadata:
  type: project
---

# Run 2026-08-05 (workflow run 31017959495)

Selected tasks: 10, 2, 9 + 11. All three pre-selected tasks share one bundled PR.

## Bundled PR — `repo-assist/ipa-phase2-word-timings-2026-08-05`

Commit `f66b9fcf`, 4 files / +445 / −9, draft via safeoutputs.

- **Task 10** (Take Repo Forward) + **Task 9** (Testing Improvements): Identified the brand-new issue #527 (an-lee, "Research: Displaying IPA on the transcript") as the highest-value forward step today — it contains an explicit *Phase 2 can proceed independently and in parallel* recommendation. Shipped Phase 2 as a data-only, additive, behavior-preserving PR.
- **Task 2** (Issue Comment): engaged by shipping Phase 2 + posting a focused comment on #527 summarizing scope, what was deliberately deferred to Phase 1, and the 13 new tests.

### Changes

- **`lib/data/subtitle/transcript_line.dart`** — new `class TranscriptWord { text, startMs, durationMs }` (immutable, ms-precision, with `toJson` / `fromJson` / `==` / `hashCode`). `TranscriptLine` gains an optional `words: List<TranscriptWord>?` field. `toJson` omits the key when null/empty; `fromJson` returns null when the key is absent. Equality / hashCode extended element-wise over the words list.
- **`lib/features/asr/domain/asr_timeline_builder.dart`** — `_groupFromWords` now attaches spans (with ASR leading-whitespace stripped from word text). `_groupFromSegments` attaches one span per segment (carries original timing). `_distributePlainText` leaves `words` null.
- **`test/features/asr/domain/asr_timeline_builder_test.dart`** — 13 new tests in two new groups:
  - **Word-span preservation (#527 Phase 2)** — 7 tests: ms-rounded timing per word, leading-whitespace strip, no leakage across break points, multi-segment word flattening, CJK terminator break, segment-fallback one-span-per-segment, plain-text leaves null.
  - **JSON round-trip / value equality** — 6 tests: full round-trip preserves words; pre-Phase-2 payloads decode with `words: null`; empty `words` list → null; `toJson` omits key when no spans; equality distinguishes with vs without words; element-wise equality + hash match; element-wise inequality; `words: null` on both sides compares equal.
- **`CHANGELOG.md`** — entry under `[Unreleased]` documenting the additive field, with references to issue #527 and ADR-0058.

## Task 11 — Monthly activity summary #522 refreshed

- Added the new PR review as the 4th Suggested Actions bullet (after the existing three pending PRs).
- Added a "Check comment" bullet for #527 (engagement was on a brand-new issue).
- Prepended the 2026-08-05 15:02 UTC run entry at the top of Run History.
- Future Work additions:
  - "Issue #527 follow-up: once the Phase 2 PR merges, Phase 1 (IPA overlay per word) becomes unblocked" — explicit enumeration of the four pieces Phase 1 still needs (extend `TranscriptWord` with `ipa?`, Worker KV cache + LLM endpoint, word-segmenting span builder, Settings toggle).
- Kept carry-over perf candidates and `asr_generation_job.dart` testing note (out of scope for this run).

## Verification

- Branch created: `repo-assist/ipa-phase2-word-timings-2026-08-05`.
- Diff: `4 files changed, 445 insertions(+), 9 deletions(-)`.
- `safeoutputs create_pull_request` + `safeoutputs add_comment` + `safeoutputs update_issue` all succeeded.
- `dart format` / `flutter analyze` / `flutter test` not run on the agentic runner (read-only SDK mount; same invariant #501 / PR #503 documents).

## Backlog cursor

- `comments_made`: 1 (issue #527).
- `prs_created`: 1 (`repo-assist/ipa-phase2-word-timings-2026-08-05`).
- `monthly_summary`: #522 refreshed.

## Why

- **Task 10**: Issue #527 was authored today and explicitly says *"Phase 2 can proceed independently and in parallel — it's a pure simplification of `asr_timeline_builder.dart` and an extension of the `TranscriptLine` JSON."* Shipping it now unblocks Phase 1 (IPA overlay per word) without forcing Phase 1's architectural decisions through this PR.
- **Task 9**: The new model needs to be pinned by tests before downstream consumers can trust it. 13 tests cover both the producer (timeline builder) and the contract (JSON / equality) sides end-to-end.
- **Task 2**: A brand-new research issue with a clear recommended action is the canonical engagement opportunity. Re-engaging #309 / #310 (prior 2026-07-13 comments, no human activity) would be noise.
- **Task 11**: Adds the new PR + comment to the maintainer's review queue and lays the groundwork for Phase 1 follow-up.
