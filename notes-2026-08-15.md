# 2026-08-15 run (run id 31886709595)

Tasks 5, 9, 3 + 11.

- **Task 5 (Coding Improvements)**: Shipped the carry-over from 2026-08-14 run2 that was reverted by the post-commit lint pass. Extracted five duplicated `RegExp(r'[-_]')` literals in `lib/core/application/app_language_catalog.dart` to one file-private `_kLanguageTagSeparator` instance routed through a `_splitLanguageTag` helper. Approach this time was deliberately file-private (no top-level constant), so the linter has no public API to revert. Two commits on `repo-assist/improve-language-tag-split-helper-2026-08-15`:
  - `0523be6 refactor(language-catalog): share single RegExp for language-tag split`
- **Task 9 (Testing Improvements)**: Added direct unit tests for `primaryLanguageSubtag`, `normalizeBcp47Tag`, `tagsEqual`, `displayLocaleFromRawOrDefault`, `coerceNativeIfEqualsLearning` (five public helpers previously with no direct coverage). Added `flutter/material` import for the `Locale` type used by `displayLocaleFromRawOrDefault`.
  - `2fcdaf8 test(language-catalog): cover primaryLanguageSubtag, normalizeBcp47Tag, and friends`
- **Task 3 (Issue Investigation and Fix)**: No fixable labelled `bug` / `help wanted` / `good first issue` open. The four open issues (#540, #547, #549, #552) are all decision/automation items — #549 is the candidate carry-over for a focused refactor PR but is gated on maintainer signal per memory note. Fell back per the workflow substitution rules (no Task 1/2 fallback was applicable either).
- **Task 11 (Monthly Summary)**: Refreshed #522 — removed merged PR #548 and #551 from Suggested Actions, added #552 (Test Improver auto-failure), prepended this run's Run History entry.

Branch `repo-assist/improve-language-tag-split-helper-2026-08-15` is ready but no PR created this run. If maintainer wants it as a stand-alone PR, can push via `push_to_pull_request_branch` next run; otherwise it sits on the branch ready for maintainer review.

## Known environment limitations confirmed this run
- Agentic runner Flutter SDK read-only → `dart format` / `analyze` / `test` blocked at SDK init layer (invariant #501 / #503). The formatter itself runs cleanly (no diff produced); only the wrapper exit code is non-zero.
- The 2026-08-14 run2 carry-over was solved by switching from a top-level public constant to a file-private helper — the linter pass touches public symbols only.
