<hash>size:4041</hash>

# `lib/features/discover/data/youtube_url_parser.dart`

- Parses YouTube channel URLs/IDs, `@handles`, user routes, and playlist URLs/IDs.
- Produces canonical source metadata and Worker RSSHub feed URLs.
