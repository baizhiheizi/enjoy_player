---
name: run-2026-07-29
description: Notes from Repo Assist run on 2026-07-29 (run id 30420782272) — issue #501 (setup-flutter load-bearing comment) plus three Task 8 perf candidates queued for next run.
metadata:
  type: project
---

# Run 2026-07-29 (workflow run 30420782272)

Selected tasks: 8, 10, 2 + 11.

## Task 10 — Issue #501 implemented
- Branch `repo-assist/setup-flutter-load-bearing-note-2026-07-29`, commit `609ff69`. YAML comments only — 8 lines above bash `flutter --version` (line 103), 7 lines above pwsh `flutterBat --version` (line 151). Draft PR via safeoutputs (PR number pending apply).

## Task 2 — Issue investigation and comment
- Commented on #501 with acknowledgement + PR link (temporary_id `aw_XV5TsizC`).
- #309 / #310 still have prior Repo Assist comments, no new human activity since 2026-07-13.

## Task 11 — Monthly activity summary #165 refreshed
- Removed now-merged PR #499 (perf) + PR #500 (sync_types) items; both landed cleanly 2026-07-28 (commits 868aec4, f256332, b2dc4c9, c04a6b3).
- Added correction parenthetical to the 2026-07-28 Run History entry.
- Added new setup-flutter load-bearing comment PR to Suggested Actions.
- Prepended this run's entry at the top with link to run 30420782272.

## Task 8 — Performance improvements (Explore agent result, PRs queued for next run)

Three concrete PR-shape candidates identified; all LOW-risk pure refactors; implementation deferred to next run as this run was already at the summary stage when the agent returned.

**PR-A** `perf(discover): cache per-locale DateFormat in DiscoverFeedTile`
- `discover_feed_tile.dart:201-217` — `_formatPublishedLabel` constructs `DateFormat.jm` / `MMMd` / `yMMMd` per call (~1 KB ICU each). 6–12 tile feed → 6–36 allocs/rebuild.
- Fix: static `Map<String, ({DateFormat jm, DateFormat mmmd, DateFormat yMMMd})>` lazily populated by locale. Locale doesn't change at runtime.

**PR-B** `perf(player): disableAnimationsOf() in transport bar AnimatedSwitcher`
- `global_transport_bar.dart:451` — `MediaQuery.of(context).disableAnimations` registers dep on full MediaQuery; transport bar rebuilds on keyboard / padding / DPI ticks (the Windows DPI ticks an-lee explicitly cited in PR #491).
- Fix: `MediaQuery.disableAnimationsOf(context)`. One-line. Aligned with pattern already in `enjoy_tappable.dart:43`, `skeleton.dart:70/80/105`, `library_screen.dart:144`.

**PR-C** `perf(transcript): hoist inline RegExp allocations` (bundle of 4)
- `global_transport_bar.dart:39-42` `_formatRateCore` → `_kTrailingZeros`
- `transcript_line_alignment.dart:76` `echoReferencePlainText` → `_kTagStrip` (same `r'<[^>]*>'` already in `transcript_markup.dart:8`)
- `vocabulary_context_builder.dart:23-28` `plainCueText` → two top-level regexes (called 7× per vocabulary lookup = 14 compiles per text-selection)
- `auto_translate.dart:200` `normalizeAutoTranslateSourceText` → `_kWhitespaceCollapse`
- Fix: top-level `final RegExp _kFoo = RegExp(r'...');` per site.

## Verification
- Branch verified, diff clean (17/-0, behavior-preserving). safeoutputs PR create + add_comment + update_issue + push_repo_memory all succeeded. Format/analyze/test not run; YAML only, and the agentic runner's read-only SDK is exactly the invariant #501 documents.

## Linter working-tree revert (recurring, confirmed benign)
- After commit + safeoutputs PR create, a linter pass reverted the YAML comments on `main`'s working tree.
- Branch `repo-assist/setup-flutter-load-bearing-note-2026-07-29` (commit `609ff69`) still contains the comments intact — `git show <branch>:.github/actions/setup-flutter/action.yml` confirms.
- Same pattern as 2026-07-28 (PR #499, #500). Both of those PRs still merged cleanly — the safe-outputs record is the source of truth. Per the system reminder, the revert is intentional. Do NOT re-apply.

## Backlog cursor
- `comments_made`: 1 (issue #501).
- `prs_created`: 1 (`repo-assist/setup-flutter-load-bearing-note-2026-07-29`, closes #501).
- `working_trees_reverted`: 1 (cosmetic).
- `monthly_summary`: #165 updated.
- `perf_pr_queue`: PR-A, PR-B, PR-C (Task 8 next run).
- `prior_worry_resolved`: PR #499 / #500 both merged 2026-07-28.
- `runner_fix_pending`: `baizhiheizi/gh-sr@5a8ae78`.

## Why
- **Task 10**: an-lee's #501 is the most concrete maintainer signal we have; maps perfectly to "Take the Repository Forward".
- **Task 2**: Maintainer instruction as engagement opportunity.
- **Task 11**: Removing two stale items keeps the mental model honest; the unfounded linter-revert worry deserves a correction.
- **Task 8**: Three clean candidates surfaced; implementation deferred to next run as the agent returned late in the run and the run needed to wrap without risking incomplete work.
