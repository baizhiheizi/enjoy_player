---
name: run-2026-08-01
description: Notes from Repo Assist run on 2026-08-01 (run id 30703178584) — bundled perf PR (regex hoist in json_feed_parser + subtitle_parser + community_activity_avatars; transport_volume_button .select()).
metadata:
  type: project
---

# Run 2026-08-01 (workflow run 30703178584)

Selected tasks: 2, 3, 8 + 11.

## Task 8 — Performance Improvements: bundled perf PR
- Branch `repo-assist/perf-regex-hoist-vol-select-2026-08-01`, commit `9b427e07`. 4 files / +46 / -21.
- **PR-A** `lib/features/discover/data/json_feed_parser.dart` — hoist 3 inline `RegExp`s (`[?&]v=…`, `^([a-zA-Z0-9_-]{11})$`, `youtu.be/…`) into top-level `final _kYTUrlV` / `_kYTBare` / `_kYTShort`. `extractVideoId` runs once per `FeedEntry` during `JsonFeedParser.parse()`; saves 3 allocs/entry on subscription refresh.
- **PR-B** `lib/data/subtitle/subtitle_parser.dart` — hoist 3 inline `RegExp`s (`^\d+$` SRT cue-index, SRT arrow `HH:MM:SS,mmm --> HH:MM:SS,mmm`, VTT arrow `MM:SS.mmm --> MM:SS.mmm` with optional hours) into top-level finals. Reuse the existing `tagStripRegExp` from `subtitle_markup_parser.dart` (was inlined as `r'<[^>]+>'` in VTT `_stripTags`). ~3 allocs/cue on SRT/VTT import on the UI isolate.
- **PR-C** `lib/features/player/presentation/widgets/transport/transport_volume_button.dart` — scope both `ref.watch(playerPreferencesCtrlProvider)` calls (line 91 + line 134 inside overlay `Consumer`) to `.select((p) => p.volume)`. The button only ever consumed `prefs.volume` (the `tooltip` and `icon` checks); the broad watch triggered a full rebuild on every theme / playback-rate / mute-defaults mutation. Matches the `.select()` pattern from PR #499 (transport bar) and PR #471.
- **PR-D** `lib/features/community/presentation/community_activity_avatars.dart` — hoist inline `RegExp(r'\s+')` in `initials()` to top-level final `_kWhitespaceRun`. Called per avatar per render.
- Draft PR created via safeoutputs (success — patch 8.6 KB / 199 lines, bundle 2.8 KB).
- `dart format` / `analyze` / `test` not run on agentic runner (read-only SDK mount fails on `engine.stamp.tmp` write — same invariant #501 / PR #503 documents).

## Task 2 — Issue Investigation and Comment
- 8 open issues scanned:
  - #309 / #310 still have detailed prior Repo Assist comments (2026-07-13), no new human activity since.
  - #72 / #82 / #144 / #166 / #189 are automation/agentic-workflow tracking issues — not engagement candidates.
  - #165 (monthly summary) updated as Task 11.
- No engagement candidates this run.

## Task 3 — Issue Investigation and Fix
- No open issues labelled `bug`, `help wanted`, or `good first issue`.
- Nothing actionable.

## Task 11 — Monthly activity summary #165 refreshed
- Added review entries for PR #515 (lite subscription tier UI, an-lee, 2026-08-01 12:16 UTC, `mergeable_state: unstable`) and the new bundled perf PR.
- Updated Future Work with three new perf candidates (candidate #3 `_VirtualItems` rebuild, candidate #5 per-line post-frame translate requests, `_VideoThumbnail`/`_ChannelAvatar` `Image.network` → `CachedNetworkImage`).
- Prepended this run's entry at the top with link to run 30703178584.

## Verification
- Branch verified, diff clean (4 files / +46 / -21, all behavior-preserving).
- `safeoutputs create_pull_request` + `update_issue` succeeded.

## Backlog cursor
- `comments_made`: 0 (no engagement candidates).
- `prs_created`: 1 (`repo-assist/perf-regex-hoist-vol-select-2026-08-01`, bundled perf).
- `monthly_summary`: #165 refreshed.
- `prior_pr_landed`: PR #504 merged 2026-07-30.

## Why
- **Task 8**: Four concrete regex/ref.watch candidates surfaced cleanly. Bundled into a single PR per the established pattern from PR #504. Each individual change is too small to justify its own review, and they share the same motivation. The `tagStripRegExp` dedupe in PR-B aligns VTT stripping with all 5 other call sites that already use the shared regex.
- **Task 2**: No new human activity on any candidate issue — re-engaging would be noise.
- **Task 11**: PR #515 is a fresh maintainer PR with `mergeable_state: unstable` that deserves a review entry; the three new perf candidates (deferred from this run) are noted for future work.
