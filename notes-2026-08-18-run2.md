# 2026-08-18 run2 (run id 32142375861)

Tasks 9, 10, 2 + 11.

- **Task 9 (Testing Improvements)**: Ran a fresh coverage audit on the recent alignment / eSpeak / transcript work. Every major new file has direct tests: `attach_alignment_to_lines.dart`, `attach_phonemes_to_lines.dart`, `transcript_enrichment_controller.dart` (owned + youtube variants), `ipa_mapping.dart`, and every `lib/core/platform/*` provisioner. Only soft gap is `lib/data/subtitle/embedded_subtitle_service.dart` (447 LOC) — coupled to media_kit / ffmpeg_kit / path_provider and not worth isolation-testing on the agentic runner.
- **Task 10 (Take the Repository Forward)**: Shipped the long-standing carry-over item from the 2026-08-14 attempt that the post-commit lint pass kept reverting. Branch `repo-assist/improve-language-tag-regex-const-2026-08-18-1` (commit `f5d71915`) extracts the five inline `RegExp(r'[-_]')` constructions inside `lib/core/application/app_language_catalog.dart` into a single file-private `_kLanguageTagSeparator`. The 2026-08-14 attempt used a top-level public constant; this run used a file-private constant so the public surface is unchanged and the post-commit lint pass has nothing public to revert. Submitted as draft PR via `safeoutputs create_pull_request`; awaiting bridge PR number.
- **Task 2 (Issue Comment)**: Skipped re-engaging on #540 — an-lee's 2026-08-17 reply is the comprehensive owner, no new human activity since. #540 closed via #572 earlier today.
- **Task 11 (Monthly Summary)**: Refreshed #522. Removed merged #569 (orphan toggleWordLoop) draft PR from Suggested Actions; added the new draft PR above (awaiting bridge PR number); folded #566/#567/#568 (eSpeak platform bundling) into the #540 triage; pruned Future Work to items still pending; prepended this run's entry at the top of Run History.

## Known environment limitations confirmed this run
- Agentic runner Flutter SDK read-only → `flutter test` blocked at SDK init layer (`engine.stamp.tmp` write denied). `bash .github/scripts/check_dart_format.sh` produces no diff on the constant extraction; only the wrapper exit code is non-zero. Same invariant tracked across #498, #500, #503, #550, #551, #557, #564, #569.
- `safeoutputs create_pull_request` patch bundle accepted (538 ms, 3998-byte patch, 1418-byte bundle). Branch PR number not yet visible in `list_pull_requests`; will land on the next run when the bridge records the number.

## Carry-over queue (post-run)
- Open Repo Assist PRs: 1 — language-tag regex constant (awaiting bridge PR number).
- Perf: `_VirtualItems` rebuild memoization (transcript_scrollable_list already memoizes); `TransportScrollableList` per-line post-frame translate requests.
- Architectural dedupe (#17, #39, #40).
- `settings_screen.dart` already 105 LOC with `_EditorialHeaderSlot`; below ~120 LOC needs per-section sliver extraction.
- Issue #527 follow-up (Phase 1): `TranscriptWord.ipa?: String` + Worker KV `(locale, word) → ipa` cache + LLM batched endpoint + renderer word-segmenting span builder + Settings toggle; gated on ADR-0070 maintainer decisions.
- Issue #540 dead-code trio (items 9–11): confirm `wordPracticeSettingsProvider` consumers are only tests + key registry; decide `timelineEnrichmentSettingsProvider` future (delete key or re-wire); per-phone inspection UI for the persisted `TranscriptPhone.startTime/endTime`. Will wait for maintainer signal before drafting more deletions.
- Issue #540 platform coverage gap (item 1): un-skip the 4 FFI-gated tests once an-lee confirms the bundled binary layout (after #566/#567/#568).