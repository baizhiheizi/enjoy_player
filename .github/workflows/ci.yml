name: CI

on:
  pull_request:
  push:
    branches: main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  analyze-test:
    name: Analyze & test (managed)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: ./.github/actions/setup-flutter

      - name: Flutter pub get
        run: flutter pub get

      - name: Forbid new local-path packages
        run: bash .github/scripts/check_no_new_path_deps.sh

      - name: Check for plaintext secrets in tracked files
        run: bash .github/scripts/check_secrets.sh

      - name: Dart format
        run: |
          set -euo pipefail
          paths=(lib test)
          for pkg in packages/*/; do
            [ -d "${pkg}lib" ] && paths+=("${pkg}lib")
            [ -d "${pkg}test" ] && paths+=("${pkg}test")
          done
          dart format --output=none --set-exit-if-changed "${paths[@]}"

      - name: Flutter analyze
        run: flutter analyze

      - name: Flutter test (app)
        run: flutter test

      - name: Flutter test (path packages)
        run: |
          set -euo pipefail
          for pkg in packages/*/; do
            if [ -d "${pkg}test" ]; then
              (cd "$pkg" && flutter pub get && flutter test)
            fi
          done
