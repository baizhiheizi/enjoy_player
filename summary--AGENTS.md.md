<hash>size:5027</hash>

# `AGENTS.md`

- Repository rules: native Flutter only, Riverpod, Drift-only SQLite, `logNamed` instead of `print`, and one `media_kit` player owner.
- Defines MVP scope, lookup language catalog boundaries, codegen requirements, and CI-equivalent verification commands.
