---
name: run-2026-07-11
description: Notes from Repo Assist run on 2026-07-11 (run id 29155525441) — ADR ID collision fix (Task 5)
metadata:
  type: project
---

# Run 2026-07-11 (workflow run 29155525441)

Selected tasks: 3, 4, 5 + 11.

**Task 3 (Issue Investigation and Fix)** — Fell back to Task 2. Search for open issues labelled `bug`, `help wanted`, or `good first issue` returned zero candidates. Open issues #72/#82/#144/#165/#166/#189/#296 are all automated/system/aggregator issues (no human-authored bug/feature/question issues).

**Task 4 (Engineering Investments)** — No actionable improvements identified this run:
- `.editorconfig` already on main via PR #245.
- Local CI gate scripts (`check_dart_format.sh`, `check_codegen_drift.sh`, `validate_ci_gates.sh`) added in commit `3244c3c`.
- `setup-flutter` composite action already does self-hosted runner disk caching.
- Dependency bumps: `pubspec.yaml` shows several pre-release deps (`file_picker: 12.0.0-beta.4`, `auto_updater: 0.2.1`, `ota_update: 7.1.0`, all `ai_sdk_*` at `1.1.0`) intentionally pinned exactly per ADR-0029 supply-chain risk.

**Task 5 (Coding Improvements)** — Found a real documentation integrity bug in the ADR index:

The repo's `docs/decisions/README.md` index had two duplicate ADR numbers:

- **`0021`** was used by both `0021-youtube-discover-rss.md` (2026-05-28) and `0021-multi-language-lookup-catalog.md` (2026-07-08). The latter was added by the multilang lookup feature (#139) and silently collided.
- **`0030`** had `0030-flutter-lints-baseline-no-custom-lint.md` (2026-06-29) on the index, but `0030-craft-from-text-import.md` (2026-07-09) was added silently with the same number AND never linked from the index at all.

Branch `repo-assist/fix-adr-id-collisions-2026-07-11` (commit `79750d3`):
- Renumbered the later-colliding ADRs to the next free slots in chronological order:
  - `0021-multi-language-lookup-catalog.md` → `0042-multi-language-lookup-catalog.md`
  - `0030-craft-from-text-import.md` → `0043-craft-from-text-import.md`
- The earlier 0021 / 0030 entries (YouTube discover RSS, flutter_lints baseline) keep their original numbers — they are the canonical entries on the index.
- Updated cross-references in 18 files: `AGENTS.md`, `CHANGELOG.md`, `docs/decisions/README.md`, `lib/features/craft/README.md`, `specs/005-multilang-lookup/{plan,tasks,research,quickstart}.md`, `specs/010-craft-from-text/{plan,spec,tasks,quickstart,checklists/requirements}.md`, `specs/011-craft-studio-redesign/{plan,spec,tasks}.md`.
- `openspec/changes/archive/2026-06-30-expand-learning-languages/{design,proposal}.md` references to `ADR-0021` already pointed at the YouTube Discover RSS entry; those refs are now unambiguous since the lookup-catalog ADR moved to 0042.

## Verification
- `grep -E "^\| \[" docs/decisions/README.md | grep -oE "\[\d{4}\]" | sort | uniq -d` → 0 hits.
- `ls docs/decisions/ | grep -E "^[0-9]{4}-" | sed 's/-.*//' | sort | uniq -d` → 0 hits.
- `grep -rnE "0021-multi-language-lookup-catalog|0030-craft-from-text-import" --include="*.md" --include="*.dart" .` → 0 stale hits (excluding the legitimate 0021-youtube-discover-rss.md / 0030-flutter-lints-baseline-no-custom-lint.md that survived, and the new 0042 / 0043 files themselves).
- Docs-only: no Dart, no codegen, no CI gates affected (`check_dart_format.sh` only touches `lib/`, `test/`, `packages/*/lib` + `packages/*/test`; `check_codegen_drift.sh` only runs `build_runner`).
- PR-fallback patch: `/tmp/gh-aw/aw-repo-assist-fix-adr-id-collisions-2026-07-11.patch` (32,335 bytes, 388 lines).

**Task 11 (Monthly Activity Summary)** — Replaced body of #165 with this run's new ADR collision fix suggested action, added the run to Run History, refreshed the Future Work section to note the merged ADRs #287–#295 absorbed every natural refactor target. Updated `[aw] ...` triage list to include #296 (Update Docs failure). Body 9,650 bytes (under the 10 KB safe-output limit).

## Backlog cursor
- `comments_made`: unchanged this run.
- `prs_attempted` (this run): **`repo-assist/fix-adr-id-collisions-2026-07-11` (commit 79750d3)`** — Task 5. PR-fallback recorded.
- `monthly_summary`: #165 (July) updated this run.

## Why
- Task 5: The ADR index is the project's single source of truth for architectural decisions; having two entries share `0021` and a third entry unindexed under `0030` is a real bug. The fix is mechanical and surgical — renumber two files, update ~12 cross-references, fix the index. No design judgement involved; the maintainer's original numbering intent was clear from chronology (each later ADR just didn't check for collisions). The renumbered ADRs (0042 / 0043) keep all their content and rationale; only the file number and header change.