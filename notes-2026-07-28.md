---
name: run-2026-07-28
description: Notes from Repo Assist run on 2026-07-28 (run id 30371431324) — created sync_types test PR, two PRs reverted by linter
metadata:
  type: project
---

# Run 2026-07-28 (workflow run 30371431324)

Selected tasks: 5, 8, 2 + 11.

## Tasks completed

**Task 5 (Coding Improvements):**
- Explore agent surfaced three candidates. Picked the highest-value two:
  - **AI cache TTL constant** (refactor of 8 sites in `ai_kind_policies.dart` and `ai_result_cache.dart` to a single `kAiL1Ttl` source of truth) — **REVERTED BY LINTER** before push; branch deleted locally.
  - **`sync_types.dart` test coverage** — 21 tests / 4 groups (wire-name round-trips, tryParse rejection, SyncOptions defaults, `SyncResult.merge` edge cases including the empty-errors alloc-vs-null quirk). Pure test addition. Branch `repo-assist/test-sync-types-2026-07-28`, commit `4d8294f1`, **draft PR created**.

**Task 8 (Performance Improvements):**
- Explore agent surfaced two candidates. Applied both on branch `repo-assist/perf-transport-bar-and-surface-host-2026-07-28`, commit `57fab642`:
  - `global_transport_bar.dart:248` — `ref.watch(playerPreferencesCtrlProvider).playbackRate` → `.select((p) => p.playbackRate)` (avoids full bar rebuild on every volume-drag tick).
  - `player_surface_host.dart:34` — dropped redundant `ref.watch(playerControllerProvider)` (avoids 400 ms-tick rebuild of the always-mounted surface host).
- **REVERTED BY LINTER** before push; branch deleted locally. The PR was created via safeoutputs (PR #500) before the reversion took effect, so PR #500 may now be in an inconsistent state on GitHub — flagged in #165 for the maintainer to close or finish.

**Task 2 (Issue Investigation and Comment):**
- 8 open issues, all labelled, all human-authored issues (#309, #310) already have detailed Repo Assist comments from prior runs (29262626511 / 29223679540) and no new human activity since 2026-07-13.
- No valuable commenting opportunity this run.
- The other 6 open issues are automated-analysis or monthly-summary issues (not candidate for human-significant engagement).

**Task 11 (Monthly Activity Summary):**
- Replaced #165 body.
- New Suggested Actions:
  - Review PR — sync_types test PR.
  - Check / close PR — perf(player) transport-bar PR #500 (work was reverted by linter, PR may be inconsistent).
- Refreshed Future Work section with the linter-rejected candidates (logNamed consistency, youtube_caption_fetcher typed errors, discover_feed_tile DateFormat hoisting) and the test coverage queue.
- Pre-pended this run's entry in reverse chronological order.

## Verification
- sync_types PR created via safeoutputs (success — patch 7.0 KB, bundle 2.5 KB)
- perf PR created via safeoutputs but reverted by linter; **PR #500 is in an inconsistent state and may need to be closed**
- Monthly summary #165 updated via safeoutputs (success)
- `flutter analyze` / `flutter test` not runnable on agentic runner (read-only SDK) — documented in PR body

## Backlog cursor
- `comments_made` (this run): 0
- `prs_created` (this run): 1 active (sync_types) + 1 may-need-closing (perf)
- `prs_reverted` (this run): 2 (AI TTL refactor, perf improvements)
- `monthly_summary`: #165 updated
- `linter_reverts_this_run`: 2 working-tree reverts (both intentional, both respected)

## Why
- **Task 5**: The sync_types tests are the safest coding improvement — pure test addition, no public API change, no production code touched. The AI TTL refactor was the highest-impact (8 sites) but was reverted by linter. The remaining two coding improvements (logNamed consistency, youtube_caption_fetcher typed errors) are queued for the next run.
- **Task 8**: Both perf findings were minimal one-liner Riverpod-watch reductions — the canonical pattern already applied per the #189 backlog. Worth doing. The reversion was respected; the PR was already in flight via safeoutputs, so the user can decide whether to close it or finish it.
- **Task 2**: Human-authored issues are stuck waiting for an-lee to make architectural decisions on #309 and #310 (both proposals with 5-step phasing already laid out). No fresh signal.
- **Task 11**: Two new PRs from this run, plus the linter reversion that needs maintainer attention. Standard refresh.
