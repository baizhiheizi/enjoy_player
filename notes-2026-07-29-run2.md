---
name: run-2026-07-29-run2
description: Notes from Repo Assist run on 2026-07-29 15:00 UTC (run id 30463357360) — bundled perf PR (regex hoist + MediaQuery scope + per-locale DateFormat cache).
metadata:
  type: project
---

# Run 2026-07-29 15:00 UTC (workflow run 30463357360)

Selected tasks: 2, 3, 5 + 11.

## Task 5 — Coding Improvements: bundled perf PR
- Branch `repo-assist/perf-regex-hoist-disableanim-dateformat-cache-2026-07-29`, commit `b233b322`.
- 7 files / +59 / -19, all behavior-preserving:
  1. `subtitle_markup_parser.dart` — expose `tagStripRegExp` / `whitespaceSplitRegExp` (drop `_`); drop the duplicate `_tagStripRegExp` in `transcript_markup.dart`; route all 6 call sites through the shared constants (`transcript_markup.dart`, `transcript_line_alignment.dart`, `vocabulary_context_builder.dart`, `auto_translate.dart`, plus 2 internal uses).
  2. `global_transport_bar.dart`:
     - hoist `_formatRateCore`'s `RegExp(r'\.?0+$')` to file-level `final _kTrailingZerosRegExp`.
     - `MediaQuery.of(context).disableAnimations` → `MediaQuery.disableAnimationsOf(context)` on the transport bar `AnimatedSwitcher` (drop full MediaQuery dep; matches pattern in `library_screen.dart`, `skeleton.dart`, `enjoy_tappable.dart`, `transcript_blur_text.dart`).
  3. `discover_feed_tile.dart` `_formatPublishedLabel` — per-locale `_DiscoverFeedTileDateFormats` cache (was building 3 DateFormat instances per tile per rebuild; locale doesn't change at runtime).
- PR created via safeoutputs (PR number pending apply).
- `dart format` / `analyze` / `test` not run on agentic runner (read-only SDK mount fails on cache-write step — same invariant #501 / PR #503 documents).
- Grep verified no remaining `_tagStripRegExp` / `_whitespaceSplitRegExp` references.

## Task 2 — Issue Investigation and Comment
- 8 open issues scanned. No engagement candidates:
  - #309 / #310 still have detailed prior Repo Assist comments, no new human activity since 2026-07-13.
  - #72 / #82 / #144 / #166 / #189 are automation/agentic-workflow tracking issues — not for engagement.
  - #165 (monthly summary) updated as Task 11.
- Skipped the selection's fallback (Task 2 → Task 1) — labelling task is N/A (unlabelled_issues = 0 per task_selection.json).

## Task 3 — Issue Investigation and Fix
- No issues labelled `bug`, `help wanted`, or `good first issue` to fix.
- The queued Task 8 perf candidates from 2026-07-29-run1 (PR-A/B/C) were implemented as a single bundled PR under Task 5 (more cohesive: all are "avoid redundant work in hot paths").

## Task 11 — Monthly activity summary #165 refreshed
- Replaced entire body with the standard format.
- Suggested Actions: removed the setup-flutter load-bearing comment PR (#501 was closed by an-lee's PR #503, merged 2026-07-29 09:57 UTC); added the new bundled perf PR.
- Future Work: removed the `_formatPublishedLabel` DateFormat cache entry — now implemented.
- Prepended this run's entry at the top with link to run 30463357360.

## Verification
- Branch verified, diff clean (7 files / +59 / -19, all behavior-preserving).
- `safeoutputs create_pull_request` + `update_issue` succeeded.
- **Linter working-tree revert (recurring, confirmed benign)**: after commit + safeoutputs PR create, the linter pass reverted the `main` working tree (all 7 files back to pre-change). Branch `repo-assist/perf-regex-hoist-disableanim-dateformat-cache-2026-07-29` (commit `b233b322`) still contains the changes intact — same pattern as 2026-07-28 (PR #499, #500) and 2026-07-29-run1 (setup-flutter load-bearing note). All three landed cleanly (#499 / #500 merged by an-lee on 2026-07-28; the maintainer's #503 merged on 2026-07-29 and superseded the setup-flutter draft PR). The safe-outputs record is the source of truth. Per system reminder, do NOT re-apply.

## Backlog cursor
- `comments_made`: 0 (no engagement candidates this run).
- `prs_created`: 1 (`repo-assist/perf-regex-hoist-disableanim-dateformat-cache-2026-07-29`, PR number pending apply).
- `monthly_summary`: #165 refreshed.

## Why
- **Task 5**: Three queued perf candidates from the prior run + the dedupe opportunity (two `_tagStripRegExp` definitions in different files) bundled into a single PR. Pure refactors, no behavior change, matches an-lee's PR #499 / #491 perf-batching style.
- **Task 2**: No new human activity on any candidate issue — re-engaging would be noise.
- **Task 11**: Cleanup of stale setup-flutter PR entry (closed by maintainer's #503) keeps the action list honest; the new PR needs review.