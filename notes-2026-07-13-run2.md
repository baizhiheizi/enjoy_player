---
name: run-2026-07-13-run2
description: Notes from Repo Assist run on 2026-07-13 (run id 29262626511) — word-recall #309 comment, codegen-drift issue, dead-code removal PR
metadata:
  type: project
---

# Run 2026-07-13 15:39 UTC (workflow run 29262626511)

Selected tasks: 2, 4, 10 + 11.

**Task 2 (Issue Investigation and Comment)** — Posted `#aw_utSp5wsV` on **#309** ([shadow-reading] Word-recall feedback when pronunciation assessment is unavailable). Verified every R1–R4 citation against `main` (`b65450f`):
- **R1** `recording_assessment_controller.dart:111-115` + `stub_ai_capabilities.dart:164-173` (ADR-0014 §5) → `UnimplementedError` for `AIProvider.local`; only Azure worker / Azure BYOK / implemented provider reach feedback.
- **R2** `azure_assessment_runner.dart:184-225` already consumes `AzureWordAssessment.pronunciationAssessment.errorType` (`:212`) and `displayText` (`:193`, `:221`) for telemetry. `recording_assessment_controller.dart:143-164` flattens to `pronScore` and the opaque JSON blob — the rich data is generated and then discarded as a first-class result.
- **R3** `lib/data/db/tables/recordings.dart` declares 17 columns; `recordingDao.updateAssessment` (`app_database.dart:636-648`) writes only `pronunciationScore` + `assessmentJson`. No `recognized_text` / `word_recall_score`.
- **R4** `grep -rnE "(Levenshtein|Jaccard|editDistance|lemmat)" lib/` returns 0 hits.

Surfaced the ASR wiring change is smaller than the writeup suggests: `AsrRequest` (`asr_request.dart:4-26`) already takes `audioBytes: Uint8List + filename + language` — the exact shape a recording WAV exposes. Just `File(localPath).readAsBytes()` + `asrServiceProvider.transcribe(req)` reuses the existing worker-Whisper / BYOK / `UnimplementedAsrCapability` symmetry with `assessmentServiceProvider`. Proposed five-step phasing (schema bump → pure-Dart scorer → Layer 1 surface Azure `Words[]` → Layer 2 offline path → Speech-framework follow-up). Two design nudges: (a) ja/ko/zh tokenizers can't be whitespace-based — `tinysegmenter`-style / particle heuristics / per-character-or-jieba-lite; (b) reuse `azure_assessment_runner.dart:191-193` `displayLooksEmpty` for typed `WordRecallStatus.noSpeech` so "0% recall" never shows for silence. Offered a docs-and-tests-only PR for steps 1+2.

**Task 4 (Engineering Investments)** — Filed issue flagging the **Codegen drift CI failure** blocking every push to `main`. Traced the drift to commit `10f20e4` (`fix: resolve circular dependency between apiClientProvider and authRepositoryProvider`): `auth_repository.dart:38-49` added `registerOn401RefreshCallback(...)` + `ref.onDispose(clearOn401RefreshCallback)`, which mutated the provider's hashable surface, but the `.g.dart` hash wasn't regenerated. Current `be804ff1de0d5c647ce6e2aea64b3ed47dfb3156` vs build_runner's `432b445daeeb7f25107d57d0cc916962cc27012b`. One-line maintainer fix: `bash .github/scripts/check_codegen_drift.sh --fix`. Asked in the issue if the maintainer wants me to open the PR.

**Task 10 (Take the Repository Forward)** — Identified `resolvePracticePosterHeroText` in `lib/features/share_poster/domain/practice_poster_data.dart` as pure dead code: `@deprecated` since `resolvePracticePosterQuote` shipped, with zero call sites anywhere (`grep -rn` returns only the function's own definition). Created branch `repo-assist/remove-dead-practice-poster-hero-text-2026-07-13` (commit `efca21f`) and submitted a draft PR via safeoutputs (PR-fallback since no git credentials in this sandbox — patch at `/tmp/gh-aw/aw-repo-assist-remove-dead-practice-poster-hero-text-2026-07-13.patch`). 12-line deletion, same pattern as the merged `chore-remove-dead-debounce` (#62). Test Status in the PR body notes the local Flutter SDK cache is mounted read-only in this sandbox, so `flutter analyze` / `flutter test` could not run locally; CI on the self-hosted runner will validate.

**Task 11 (Monthly Activity Summary)** — Body of #165 is still at the 10 KB safe-output ceiling (10,561 bytes). Posted comment-only update `#aw_XWe9QSxY` capturing this run's activity. Body remains the canonical reference for the rest of the July summary; older PR-fallback items remain pending review.

## Verification
- Issue citations for #309 all match `main` at `b65450f`; verified `AzureWordAssessment.pronunciationAssessment.errorType` at `azure_assessment_runner.dart:212`, `AzurePronunciationAssessmentResult.displayText` at `:193`/`:221`, `recordingDao.updateAssessment` at `app_database.dart:636-648`.
- Codegen drift identified by reading `actions/runs/29250425376/job_id=86817324812` failure log; the workflow logs the exact hash mismatch and provides the `--fix` command.
- Dead code removal: `grep -rn resolvePracticePosterHeroText lib/ test/ packages/` returns only the function's own definition at line 195. 12-line deletion; `git diff --stat` confirms `1 file changed, 12 deletions(-)`.
- `flutter analyze` / `flutter test` could not run locally (read-only Flutter SDK cache); PR + issue bodies document the limitation explicitly.

## Backlog cursor
- `comments_made` (this run): `#aw_utSp5wsV` on #309; `#aw_XWe9QSxY` on #165.
- `prs_attempted` (this run): `repo-assist/remove-dead-practice-poster-hero-text-2026-07-13` (commit `efca21f`) — PR-fallback recorded.
- `issues_filed` (this run): codegen-drift CI blocker (transient_id not returned; safeoutputs recorded the transaction).
- `monthly_summary`: #165 received a comment-only update this run (body still at 10 KB ceiling).

## Why
- **Task 2**: #309 is a fresh, well-scoped proposal from the maintainer with specific file references. Verified every citation and offered an actionable phasing plan plus two design nudges (CJK tokenizer + `noSpeech` typed status) that the proposal didn't address. This is the highest-value discussion on the open-issue list right now.
- **Task 4**: Codegen drift is currently blocking **every** PR touching `lib/`, `pubspec.yaml`, or `pubspec.lock`. The fix is one bash command. Filing the issue (vs. blindly opening the PR) lets the maintainer choose whether to do the trivial fix themselves or delegate.
- **Task 10**: Removing a deprecated, zero-caller helper is the lowest-risk, highest-signal kind of cleanup. The `@deprecated` directive already telegraphed the intent; this PR just executes it. Same pattern as #62 (`chore-remove-dead-debounce`) which was quickly merged.

## Related
- [[run-2026-07-13]]
- [[run-2026-07-11]]