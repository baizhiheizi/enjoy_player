---
name: run-2026-08-08
description: Notes from Repo Assist run on 2026-08-08 (run id 31235204537) — shared whitespace normalization refactor, issue triage, and monthly summary refresh.
metadata:
  type: project
---

# Run 2026-08-08 (workflow run 31235204537)

## Selected tasks

Tasks 5, 3, 2 + mandatory Task 11. Task 3 was not applicable because no open issue was labelled `bug`, `help wanted`, or `good first issue`; it used the required Task 2 fallback.

## Task 5 — Coding Improvements

Created draft PR intent `#aw_txtnorm1` on branch `repo-assist/improve-text-normalization`, commit `159c5c3`.

- Added `lib/core/utils/text_normalization.dart` with cached `whitespaceRunRegExp` and `collapseWhitespace`.
- Replaced duplicated `RegExp(r'\\s+')` normalization at ASR, Azure assessment, Craft, auto-translate, vocabulary lookup, recording assessment, and shadow-reading call sites.
- Reused the shared regex for community initials and kept `whitespaceSplitRegExp` as a compatibility alias in the subtitle parser.
- Added `test/core/utils/text_normalization_test.dart` with four focused behavior tests.
- 11 files changed, 57 insertions, 18 deletions; no dependencies or generated files changed.

## Task 2 / Task 3 fallback — Issue Investigation and Comment

Reviewed all 13 open issues from oldest to newest and read comments on #309, #310, #522, #527, and #535. No open issue had a fix label, and #309, #310, and #527 already had Repo Assist comments with no new human activity. The remaining issues are automation or tracking reports. No new comment was posted to avoid noise.

## Task 11 — Monthly summary

Replaced the body of open issue #522 with the required August 2026 format. Added pending review actions for #537 and `#aw_txtnorm1`, unacknowledged Repo Assist comment checks for #309, #310, and #527, the ADR-0070 manual-recreation decision from #535, and prepended the 2026-08-08 run history entry.

## Verification

- Standalone assertions for `collapseWhitespace`: passed.
- Isolated direct Dart formatter check for all 11 changed Dart files: passed (`0 changed`).
- `git diff --check`: passed.
- `flutter pub get`, `flutter analyze`, `flutter test`, repository format, and codegen drift commands were blocked before execution because the agentic Flutter SDK mount is read-only and Flutter attempted to write `engine.stamp.tmp` / `engine.realm`.

## Backlog

- `comments_made`: none this run.
- `prs_created`: `#aw_txtnorm1` (`repo-assist/improve-text-normalization`).
- `monthly_summary`: #522 refreshed.
- Continue monitoring the new normalization PR and existing onboarding test PR. Keep #527 Phase 1 implementation gated on maintainer decisions documented in ADR-0070.
